# React parity contract

The React portal renders the exact pre-migration HTML documents as raw source documents. No investor section, image, CSS rule, interaction, evidence link, legal text, inquiry workflow, or Knowledge Chat markup is recreated or summarized in React. The original documents are the render source. Original assets are published under `/assets/` so every legacy reference resolves unchanged.
